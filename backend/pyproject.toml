[project]
name = "boat-backend"
version = "0.1.0"
description = "Boat owner organization tool backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.1",
    "python-multipart>=0.0.9",
    "pydantic>=2.6",
    "passlib>=1.7.4",
]

[tool.uv]
package = false
